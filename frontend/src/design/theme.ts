/**
 * CricketOS Theme — Compatibility re-export
 * ============================================
 * The single source of truth for themes is `src/core/theme`.
 * This file exists only to preserve legacy `@design/theme` imports.
 */
export { lightTheme, darkTheme } from '@core/theme';
